// src/typings.d.ts
// Declara imports de CSS como side-effect para o TypeScript não reclamar.
// O next-env.d.ts gerado por `next dev/build` faz isso automaticamente,
// mas este arquivo garante que o editor não mostre erros antes do primeiro build.

declare module "*.css";
